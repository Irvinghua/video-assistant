我计划开发一款chrome 插件，借助该插件，能够在bilibili、youtube类似这种长视频网站上观看视频时，无需看完整个视频，用该插件生成的该视频的文字总结，即可快速了解视频内容。同时还能按照点赞数量，总结该视频评论区的主要几个热门观点。
以下是该插件的功能需求文档：

## 🚀 视频 AI 助手：Chrome 插件功能说明清单

### 1. 核心功能：视频内容智能总结

这是插件的灵魂，目标是让用户在 1 分钟内读懂 30 分钟的视频。

* **多维度摘要生成**：
* **一句话简介**：概括视频核心主旨。
* **分段要点（Timestamps）**：自动将视频划分为几个逻辑章节，并附带时间戳链接。点击时间戳，视频自动跳转至对应位置。
* **全文精简稿**：保留视频金句和核心逻辑的 500-1000 字摘要。


* **多语言支持**：
* **自动翻译**：无论视频是英文（YouTube）还是中文（Bilibili），总结均可按用户浏览器设置的语言输出。
* **字幕抓取**：优先抓取官方字幕，若无则调用 AI 进行语音转文字（ASR）处理。
* **界面国际化（i18n）**：插件 UI 与 AI 输出均支持 10 种语言：简体中文 (zh-CN)、English、हिन्दी、Español、العربية、Français、Português、Bahasa Indonesia、日本語、한국어。
    * 首次安装时按浏览器首选语言自动匹配（loose match：`zh-*` → `zh-CN`、`pt-*` → `pt`、`ar-*` → `ar`），命中失败回退英文。
    * 用户可在「设置页 → 语言」或「侧边栏顶部地球图标」两处快速切换；切换后侧边栏 / 弹窗 / 设置页同步生效。
    * AI 输出（摘要、舆情分析、思维导图、Ask AI 回复）跟随 UI 语言，比如 UI 切到日语后下一次生成会用日语返回。
    * 阿拉伯语启用 `dir="rtl"` 布局，相关组件使用 Tailwind logical 属性（`ms-`/`me-`/`ps-`/`pe-`/`border-s-`/`border-e-`）自动镜像。
    * 已生成内容沿用旧语言缓存，用户主动「清除缓存」后重新生成即可获得新语言版本，避免无意义的 token 消耗。



### 2. 核心功能：评论区舆论洞察

不仅看博主说了什么，还要看观众在想什么。目标是输出一份结合视频本体的"舆情全景报告"，覆盖共识、分歧、信息偏离度与整体氛围。

#### 2.1 评论抽样与权重分配策略

为了让 LLM 总结出的观点不偏颇，不能仅仅抓取排在最前面的评论，需要建立多维度的抽样池：

* **高赞共识池（占抽样约 50%）**：抓取默认"按热度排序"下的前 50 条一级评论。这些通常代表了大多数网友的"共识"。评论总数不足 50 则获取全部评论。
* **争议与分歧池（占抽样约 50%）**：这是获取"分歧点"的核心。通过启发式指标定位争议评论：`争议指数 = 回复数 / (点赞数 + 1)`。当一条一级评论的点赞数一般，但下方盖起了"高楼"（回复数极高），通常意味着发生了激烈的辩论。

#### 2.2 一级与二级评论的抓取联动机制

二级评论深不见底，盲目展开会极大地拖慢插件速度并触发反爬机制。应当采用**按需展开**的策略：

* **默认忽略**：对于回复数较少（例如少于 3 条）的一级评论，直接忽略其二级评论。
* **触发展开**：只有当一级评论的"争议指数"超过设定阈值，或绝对回复数超过 10 条时，才触发二级评论的展开动作。
* **截断抓取**：展开二级评论后，不要全部抓取。只需抓取该子讨论串中点赞最高的前 5 条。这几条通常已经涵盖了反方或支持方的核心论点，继续往下抓取大多是情绪化的"水贴"或人身攻击。

#### 2.3 数据清洗与降噪过滤

发给大模型的 Token 都是成本，且无意义的信息会干扰大模型的注意力：

* **字数过滤**：剔除少于 5 个字的评论，这类评论对总结观点没有信息增量。
* **去格式化**：清理评论中的艾特信息（@用户名）、纯表情符号、时间戳跳转链接等。
* **去重机制**：如果多条评论文本相似度极高（常见于粉丝控评或玩梗刷屏），在预处理阶段可以考虑折叠或只保留一条，并标记"该观点出现频率极高"。

#### 2.4 结合视频本体的上下文对比

单纯分析评论有时会陷入断章取义的陷阱。将评论抽样与视频本身的字幕提取或 ASR 语音识别文本结合起来，可以产生质的飞跃。

将视频核心内容的文本摘要作为 Context 一起发给大模型。这样大模型不仅能总结出观众的共识和分歧，还能对比出**"创作者表达的核心"与"观众接收到的信息"**之间的落差，从而输出更高维度的情感分析报告（例如："虽然 UP 主在科普 A 技术，但评论区的焦点完全跑偏到了 B 争议上"）。

#### 2.5 LLM Prompt 结构建议

为了确保输出稳定、结构化，发给 LLM 的 Prompt 可以参考以下架构：

> **Role**: 你是一位深度社交媒体分析师，擅长从碎片化评论中通过对比视频本体内容，提炼群体心理与舆论风向。
>
> **Background Data**:
>
> 1. **视频核心摘要**: {{Video_Summary}}
> 2. **精选评论样本**:
>     * **高赞共识区**: {{Top_Comments_List}}
>     * **争议辩论区**: {{Controversial_Threads_List}}（包含一级评论及代表性二级回复）
>
> **Task**: 请结合"视频核心摘要"与"评论样本"，进行深度的共识与分歧分析。你需要识别出观众是对视频内容的哪个部分产生了共鸣，又是在哪个环节产生了质疑。
>
> **Analysis Requirements**:
>
> * **共识提取**: 找出 3 个以上被反复提及且点赞极高的观点。
> * **分歧解剖**: 针对争议区，提炼出 A 派与 B 派的核心对立点，并标注争议是源于视频本身的逻辑缺陷、立场偏见，还是观众的认知差异。
> * **信息偏离度 (The Gap)**: 对比视频摘要，指出评论区是否讨论了视频未提及但相关度极高的话题，或者视频中哪个重点被观众完全忽视了。
> * **舆情氛围**: 用 3 个关键词概括当前评论区的整体情绪（如：催更激烈、理性探讨、情绪化对立）。
>
> **Output Format (Markdown)**:
>
> ### 📊 舆情全景报告
>
> #### 🤝 核心共识 (Consensus)
>
> * **观点 A**: [简述内容] —— _支撑热度：极高/高_
> * **观点 B**: [简述内容]
>
> #### ⚡ 主要分歧 (Divergences)
>
> * **争论焦点**: [话题名称]
>     * **立场 A**: [核心论点]
>     * **立场 B**: [核心论点]
>     * **分析**: [简单说明分歧产生的根源]
>
> #### 🔍 视频内容 vs 观众反馈 (Gap Analysis)
>
> * **命中区**: 视频中 [具体内容] 最受好评，引发大规模共鸣。
> * **盲区/溢出**: 视频未提到 [某话题]，但评论区有大量延伸探讨；或观众普遍对 [某环节] 表示困惑。
>
> #### 💡 值得关注的独立见解
>
> * [提取 1-2 条高信息增量的专业评论或神回复]



### 3. 交互与 UI/UX 设计

插件要像“原生”一样长在页面里。

* **嵌入式侧边栏（Sidebar）**：
* 在视频播放器右侧（Bilibili 的弹幕列表上方或 YouTube 的推荐列表位置）展示总结界面。
* 支持**折叠/展开**，不影响正常观影。


* **暗黑模式适配**：
* 完美适配 Bilibili 和 YouTube 的原生配色主题。



### 4. 增强功能（进阶版）

* **对话式提问（Ask AI）**：
* 用户可以针对视频内容追问，例如：“视频里提到的那个工具叫什么名字？”或者“帮我详细解释下 12 分 30 秒提到的概念。”


* **知识沉淀工具**：
* **一键收藏**：支持将总结同步到 Notion、Readwise 或 Obsidian。
* **脑图生成**：自动根据视频逻辑生成一张思维导图。

### 5. 注意事项
1.给需要用到ASR功能的模型提供配置API KEY入口
2.给需要用到文本处理的模型提供配置API KEY入口
3.注意用代码实现时，函数以及功能模块之间的低耦合性。因为网页布局时常会更新，导致抓取视频、字幕和评论的方法会失效。插件此时实现联动更新，改动成本目标做到比较小。

