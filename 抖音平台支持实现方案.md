# 抖音网页版平台支持 — 详细技术方案

> 配套文档:可行性结论见 [`抖音网页版字幕音频抓取可行性分析.md`](./抖音网页版字幕音频抓取可行性分析.md)。本文是落地实现方案。
>
> 路线:**优先读取音频流 → ASR 转写成文本 → 基于文本生成摘要 + 时间轴章节导航**(仿 B 站无 CC 视频处理方式),**不采用**官方 `chapterInfo`(信息损失过大)。
>
> 制定时间:2026-06-16。

---

## 1. 目标与范围(本期)

| 功能 | 本期 | 取数方式 |
|---|---|---|
| 视频识别 / 元信息 | ✅ | URL(awemeId)+ DOM(标题/作者/封面/时长) |
| 视频摘要 | ✅ | 音频 → ASR → 文本 → `VideoSummarizer` |
| 时间轴章节 + 导航 | ✅ | 摘要产出的 `chapters[]`,点击 `seekTo` |
| 思维导图 | ✅ | 复用 subtitles 文本(ASR 结果) |
| Ask AI | ✅ | 复用 subtitles 文本 |
| **评论舆情** | ✅ | **DOM 抓取**(精选弹窗右栏 / 详情页评论区) |
| 官方 chapterInfo | ❌ 不用 | 信息损失大 |
| CC 字幕解析 | ❌ 跳过 | 实测找不到带 CC 的视频 |

---

## 2. 决策记录(brainstorming 结论)

| # | 决策点 | 结论 | 理由 |
|---|---|---|---|
| D1 | ASR 触发方式 | **手动按钮触发** | 抖音无 CC,ASR 是唯一文本源;每次转写有成本/耗时;精选是快速滑动信息流,自动跑会烧钱。与现有 B 站/YouTube 回退一致。 |
| D2 | 本期范围 | **含评论舆情** | 精选弹窗右栏评论已渲染在 DOM,有稳定 `data-e2e`,DOM 抓取成本低。 |
| D3 | swiper 切换 | **监听 modal_id 变化自动重置** | 不刷新页面,靠 `modal_id` 切视频;切换后自动 re-detect + 清空上个视频状态,但**不自动跑 ASR**,等用户点按钮。 |
| D4 | seekTo | **`window.player.seek(秒)`** | 调用 xgplayer 实例方法,语义最准;经 MAIN 世界桥执行。 |

---

## 3. 架构总览

```
┌────────────────────────── 抖音页面 (douyin.com) ──────────────────────────┐
│                                                                            │
│  MAIN world                          ISOLATED world (Plasmo 内容脚本)      │
│  ┌─────────────────┐                 ┌──────────────────────────────────┐  │
│  │ window.player    │                 │  src/contents/douyin.tsx          │  │
│  │  .config         │                 │   • SPA 监听(modal_id 变化)      │  │
│  │   .awemeInfo ────┼──executeScript──┤   • Shadow DOM 侧边栏 (复用)      │  │
│  │  .seek()         │  (world:MAIN)   │   • DouyinService                 │  │
│  └─────────────────┘       ▲         │      - detectVideo()  ← URL+DOM   │  │
│         读不到 ↑            │         │      - getComments()  ← DOM 抓    │  │
│  ┌─────────────────┐       │         │      - getAudioUrlCandidates() ──┐│  │
│  │ 评论 DOM         │←──读──┤         │      - seekTo() ─────────────┐   ││  │
│  │ [data-e2e=       │  (DOM 跨世界共享)│                              │   ││  │
│  │  "comment-item"] │                 └──────────────────────────────┼───┼┼──┘
│  └─────────────────┘                                                  │   ││
└───────────────────────────────────────────────────────────────────── │ ──│┘
                                                                         │   │ sendMessage
                                          ┌──────────────────────────────▼───▼──┐
                                          │  background.ts (Service Worker)       │
                                          │   READ_DOUYIN_PLAYER:                 │
                                          │    executeScript(world:"MAIN") 读     │
                                          │    window.player.config.awemeInfo /   │
                                          │    window.player.seek()               │
                                          └───────────────────────────────────────┘

音频下载 & ASR(复用现有 useSummary.handleDigitalASR):
  getAudioUrlCandidates() → downloadAudio(内容脚本 fetch, credentials:"omit")
    → transcribeLongAudio(Whisper) → VideoSummarizer.summarize → 摘要+章节
```

**关键约束**:
- 内容脚本(ISOLATED)**读不到** `window.player`(页面 JS 对象),必须经 background `executeScript({world:"MAIN"})` 桥读取(复用 YouTube 既有模式)。
- DOM 在两个世界间**共享**,所以评论抓取、标题/作者读取、video 元素操作都可在内容脚本直接做,无需 MAIN 桥。

---

## 4. 复用 vs 新建

### 4.1 几乎零改动复用
| 模块 | 作用 |
|---|---|
| `services/asr/ASRPipeline.transcribeLongAudio` | 音频分块 + Whisper 转写 |
| `services/summarizer/VideoSummarizer` | 文本 → 摘要 + 带时间戳章节 |
| `hooks/useSummary.handleDigitalASR` | ASR 全流程编排(getUrl→download→transcribe→summarize) |
| `useSummary.downloadAudio` | 内容脚本 fetch + `credentials:"omit"`(抖音 CDN 适配,见 §6.3) |
| `services/platform/commentSampling`(`cleanComments`/`pickControversial`/`HOT_POOL_SIZE`/`CONSENSUS_SIZE` 等) | 评论取样 |
| `hooks/useMindMap` / `AskAIPanel` | 基于 subtitles 文本 |
| `components/Sidebar/SummaryPanel` 章节 UI + seekTo 调用 | 章节导航 |
| `services/cache/CacheService` | 缓存(key 用 `douyin:{awemeId}:*`) |

### 4.2 新建
| 文件 | 内容 |
|---|---|
| `src/services/platform/douyin/DouyinService.ts` | `IPlatformService` 实现 |
| `src/services/platform/douyin/commentScraper.ts` | DOM 抓评论 → `Comment[]` → `commentSampling` |
| `src/services/platform/douyin/playerBridge.ts` | 封装 `READ_DOUYIN_PLAYER` 消息(读 awemeInfo / 调 seek) |
| `src/contents/douyin.tsx` | 内容脚本入口 + SPA 监听 |
| `src/background.ts`(改) | 新增 `READ_DOUYIN_PLAYER` / `DOUYIN_PLAYER_SEEK` handler |
| `src/services/platform/PlatformFactory.ts`(改) | 加 douyin 分支 |
| i18n `locales/*.json`(改) | 平台名等文案 |

---

## 5. 模块详细设计

### 5.1 PlatformFactory 识别
三种页面形态需全覆盖(详见可行性分析 §1.1):`/video/{id}`、`/jingxuan?modal_id=`、`/user/...?modal_id=&vid=`。

```ts
// 识别:路径含 /video/  或  URL 含 modal_id 参数
if (url.includes("douyin.com")) {
    const u = new URL(url)
    if (/\/video\/\d+/.test(u.pathname) || u.searchParams.has("modal_id")) {
        return new DouyinService()
    }
}
```

### 5.2 DouyinService(`IPlatformService`)

```ts
export class DouyinService implements IPlatformService {
    getPlatformName() { return "douyin" }

    // 同步:awemeId 来自 URL,标题/作者/封面/时长来自 DOM —— 不依赖 window.player
    detectVideo(): VideoInfo | null {
        const awemeId = extractAwemeId(window.location.href)   // /video/{id} 或 modal_id 或 vid
        if (!awemeId) return null
        return {
            id: awemeId,
            title:  readTitleFromDOM()  || "Unknown Title",     // desc 区域
            author: readAuthorFromDOM() || "Unknown Author",    // @作者
            coverUrl: "",
            duration: document.querySelector("video")?.duration || 0,
        }
    }

    // 字幕:抖音无 CC → 永远返回空,触发 ASR 回退
    async getSubtitles(): Promise<SubtitleSegment[]> { return [] }

    async getComments(videoId: string): Promise<SampledComments> {
        return scrapeDouyinComments(videoId)   // §5.5
    }

    supportsDigitalASR() { return true }

    // 异步:经 MAIN 桥读 window.player.config.awemeInfo.video.bitRateAudioList
    async getAudioUrlCandidates(videoId: string): Promise<string[]> {
        const aweme = await readDouyinPlayer(videoId)   // §5.3
        const audioList = aweme?.video?.bitRateAudioList || []
        if (audioList.length) {
            // ASR 只需低码率;选 bitrate 最低
            const picked = [...audioList].sort((a,b)=>(a.bitrate||0)-(b.bitrate||0))[0]
            const srcs = (picked.urlList||[]).map((u:any)=>u.src).filter(Boolean)  // 多 CDN
            if (srcs.length) return srcs
        }
        // 兜底:渐进式整片 MP4(含音频,~35MB)
        const prog = (aweme?.video?.playAddr?.urlList || aweme?.video?.playAddr || [])
        return toSrcArray(prog)
    }
    async getAudioUrl(videoId: string) {
        return (await this.getAudioUrlCandidates(videoId))[0] ?? null
    }

    // D4:经 MAIN 桥调 window.player.seek()
    seekTo(seconds: number): void { douyinPlayerSeek(seconds) }
}
```

字段路径(实测,见可行性分析 §2.1):
- 音频:`awemeInfo.video.bitRateAudioList[].urlList[].src`(douyinvod CDN 直链,`ACAO:*`+Range+需 Referer)
- ⚠️ **禁用** `urlList[].playApi`(同源 `/aweme/v1/play/` 返回验证码中间页,见可行性分析 §2.3)

### 5.3 background:READ_DOUYIN_PLAYER(MAIN 世界桥)

```ts
// background.ts 新增
if (message.type === "READ_DOUYIN_PLAYER") {
    const tabId = sender.tab?.id
    chrome.scripting.executeScript({
        target: { tabId }, world: "MAIN",
        func: (wantId: string) => {
            const p:any = (window as any).player
            const a = p?.config?.awemeInfo
            if (!a) return { ok:false, reason:"no-player" }
            // swiper 预加载多实例:校验当前播放器是否就是目标视频
            if (wantId && a.awemeId !== wantId) return { ok:false, reason:"id-mismatch", got:a.awemeId }
            const v = a.video || {}
            return { ok:true, video: {
                bitRateAudioList: v.bitRateAudioList,
                playAddr: v.playAddr,
                duration: v.duration,
            }, awemeId: a.awemeId }
        },
        args: [message.videoId || ""]
    }).then(r => sendResponse(r?.[0]?.result || {ok:false}))
     .catch(e => sendResponse({ok:false, error:String(e)}))
    return true
}

// seek
if (message.type === "DOUYIN_PLAYER_SEEK") {
    chrome.scripting.executeScript({
        target: { tabId: sender.tab!.id! }, world: "MAIN",
        func: (sec:number) => { try { (window as any).player?.seek(sec); (window as any).player?.play?.() } catch{} },
        args: [message.seconds]
    }).then(()=>sendResponse({ok:true}))
    return true
}
```
> ⚠️ `executeScript({world:"MAIN"})` 需要 `scripting` 权限(已有)+ host_permissions 覆盖 douyin.com(`https://*/*` 已覆盖)。**仅 background 能调 `chrome.scripting`**,故必须从 background 发起,内容脚本经 `sendMessage` 中转。

### 5.4 内容脚本 `src/contents/douyin.tsx` + SPA 监听(D3)

```ts
export const config: PlasmoCSConfig = {
    matches: ["https://www.douyin.com/*"],
    // 复用 YouTube/Bilibili 的 Shadow DOM 侧边栏挂载方式
}
```
SPA 监听(精选/主页弹窗滑动换视频):
- 抖音用 History API 改 `modal_id`,**programmatic pushState 不触发 popstate** → 需 monkeypatch `history.pushState/replaceState` + 监听 `popstate`,或轮询 `location.href`。复用 YouTube 内容脚本既有的 SPA 导航处理。
- 监测到 `awemeId` 变化 → 通知 `VideoContext` 重新 `detectVideo()` 并**清空上个视频的 summary/comments/subtitles 状态**;**不自动触发 ASR**(等用户点按钮)。

### 5.5 评论 DOM 抓取 `commentScraper.ts`(D2)

DOM 锚点(实测,见可行性分析 §4):
```
[data-e2e="comment-list"]        评论列表容器
[data-e2e="comment-item"]        单条评论
[data-e2e="video-comment-more"]  展开更多回复
```
流程(产出与 B 站一致的 `SampledComments`):
1. 滚动 `comment-list` 容器,渐进加载,直到收集到 `HOT_POOL_SIZE` 条 L1 评论或到底。
2. 每条 `comment-item` 解析:用户名、正文、`时间·地点`、点赞数、回复数。
   - 正文/点赞等在混淆类名子节点(如 `.Sbe6bqNb`),用**结构定位**(相对 `comment-item` 的层级/图标兄弟节点)而非写死类名,降低抖音改版风险。
3. `consensus = cleanComments(l1.slice(0, CONSENSUS_SIZE))`
4. `controversial = cleanComments(pickControversial(l1))`;对其点 `video-comment-more` 展开,抓 L2 回复填 `replies`。
5. 返回 `{ consensus, controversial }`。

> 说明:评论抓取在内容脚本(ISOLATED)直接读共享 DOM,无需 MAIN 桥。需处理两种容器布局(精选弹窗右栏 vs 详情页下方)——优先精选,详情页做兼容。

### 5.6 ASR 主链路(复用,几乎不改)
`useSummary.handleDigitalASR` 已是通用流程,DouyinService 实现 `getAudioUrlCandidates` 后即可直接复用:
```
getAudioUrlCandidates() → downloadAudio(urls) → transcribeLongAudio() → handleSummarize()
```
- 触发入口:SummaryPanel 现有「通过 ASR 总结」按钮(D1 手动)。需确认抖音无字幕时 UI 正确展示该按钮(`getSubtitles` 返回空 → 走 ASR 分支)。
- 音频体积:9 分钟约 13MB(192k);`transcribeLongAudio` 已支持长音频分块,无需改。

---

## 6. 关键风险与验证点

| # | 风险 | 缓解 / 待联调验证 |
|---|---|---|
| R1 | 内容脚本(ISOLATED)fetch 抖音 CDN 是否带上 `Referer: douyin.com` | B 站同模式可用;抖音 MAIN 世界实测 206 通过。**需在 ISOLATED 内容脚本上下文复测完整 13MB 下载**(§6.3) |
| R2 | swiper 预加载多 player 实例,`window.player` 可能非当前视频 | MAIN 读时校验 `awemeId===videoId`,不匹配则报错重试 |
| R3 | History pushState 不触发 popstate,漏掉切换 | monkeypatch pushState/replaceState + 轮询兜底 |
| R4 | 评论 DOM 类名混淆、改版易碎 | 用 `data-e2e` + 结构定位,不写死混淆类名 |
| R5 | CDN URL 时效(`x-expires`)/单次有效 | 下载即用;多 CDN 候选 + 失败重取 awemeInfo |
| R6 | 抖音风控:频繁 executeScript / fetch | 手动触发(D1)天然低频;不轮询音频 |
| R7 | Whisper 单请求体积上限 | 已有分块,无忧 |

### 6.3 必做联调验证(R1)
在内容脚本 ISOLATED 上下文(非 MAIN)实测:
```ts
fetch(audioSrc, { credentials: "omit" })  // 期望 200/206 + MP4(ftyp),非验证码 HTML
```

---

## 7. 实现里程碑

1. **M1 — 平台骨架**:PlatformFactory 识别 + DouyinService.detectVideo(URL+DOM) + douyin.tsx 内容脚本注入 + 侧边栏能在抖音三种页面挂载显示。
2. **M2 — 音频 ASR 主链路**:background `READ_DOUYIN_PLAYER` + getAudioUrlCandidates + 复测下载(R1)→ 跑通 ASR → 摘要 + 章节 + seekTo(`window.player.seek`)。
3. **M3 — 思维导图 / AskAI**:基于 M2 的 ASR 文本验证(预计零额外开发)。
4. **M4 — 评论舆情**:commentScraper(精选弹窗)→ 兼容详情页 → 接 CommentAnalyzer。
5. **M5 — SPA 切换 + 收尾**:modal_id 监听重置、i18n、三种页面回归测试、缓存隔离(`douyin:{awemeId}:*`)。

---

## 8. 待办 / 开放项

- [ ] R1 联调:ISOLATED 内容脚本完整下载 13MB 音频(确认带 Referer、非验证码页)。
- [ ] 确认 YouTube 内容脚本的 SPA 导航处理是否可直接复用于抖音 modal_id 监听。
- [ ] 评论详情页布局兼容(M4 次优先)。
- [ ] `detectVideo` 的标题/作者 DOM 选择器在三种页面形态下的稳定取值。
