# 抖音网页版字幕 / 音频抓取可行性分析

> 目的:评估为本扩展(Video AI Assistant)新增 **抖音网页版(douyin.com)** 平台支持的技术可行性,重点回答"能否抓取到字幕和音频"。
>
> 调研方式:Chrome DevTools MCP 对真实视频页做抓包 / 运行时取证。
>
> 调研样本:视频 `7650813418760359203`(@小天fotos「多Agent无人值守跑了4天,14万行代码,用了什么编排?」,时长 9:04)。
>
> 调研时间:2026-06-16。

---

## 0. 一句话结论

| 能力 | 结论 | 说明 |
|---|---|---|
| 视频元信息(标题/作者/简介/时长) | ✅ 容易 | 全在 `window.player` 内存对象里,免签 |
| 播放器控制(seekTo) | ✅ 容易 | `window.player.seek(秒)` 直接可用 |
| **音频 → ASR 摘要** | ✅ **可行(免签,实测通过)** | CDN 直链纯音频流,开放 CORS + 支持 Range |
| **字幕(CC)** | ⚠️ **不可靠** | 仅"挂了 CC 轨"的视频可取文字;多数视频是硬字幕,机器不可读 → 需回退 ASR |
| **官方 AI 章节总结 `chapterInfo`** | ✅ **重大利好(免签可读)** | 抖音服务端已用 ASR+LLM 生成"总述+时间轴章节+要点",直接读内存即可,见 §9 |
| 评论舆情 | ⚠️ 较费力 | 评论正文需 DOM 抓取或破 `a_bogus` 签名 |

**主链路(window.player 读取 + 音频 ASR + 摘要/思维导图/AskAI)投入产出最高且最稳,建议优先落地;字幕作为"有 `claInfo` 就用"的增强;评论放最后。**

---

## 1. 关键基础发现:数据无需破签名

抖音详情接口 `GET /aweme/v1/web/aweme/detail/` 被 **`a_bogus` + `x-secsdk-web-signature`** 双重签名保护(还伴随 `msToken` / `ttwid` / `verifyFp`),逆向成本极高且易随版本失效。

**但本扩展根本不需要自己调这个接口** —— 抖音的 xgplayer 播放器把整段视频数据挂在了一个全局对象上:

```js
window.player                          // xgplayer 实例(构造名 "s")
window.player.config.awemeInfo         // ★ 完整 aweme 详情(已解析的 JS 对象)
window.player.seek(seconds)            // 跳转播放进度(seekTo 用)
window.player.play()                   // 播放
window.player.duration                 // 时长(秒)
window.player.currentTime              // 当前进度(秒)
```

`window.player.config.awemeInfo` 的主要字段(实测):

```
awemeId, awemeType, desc, itemTitle, caption(=简介文案,非字幕),
authorInfo, stats, claInfo, video, music, textExtra,
shareInfo, status, createTime, mixInfo, seriesInfo, ...
```

> 这与现有 YouTube 读 `ytInitialPlayerResponse` 的思路一致:**内容脚本(MAIN world)直接读页面内存,绕开所有签名**。

### 1.1 取数注意:页面形态(已覆盖精选模式)

抖音有多种视频页形态,经实测 **`window.player` 数据访问在各形态下一致**:

| 形态 | URL 形如 | UI | `window.player` | 备注 |
|---|---|---|---|---|
| 规范详情页 | `/video/{id}` | 左播放器 + 下方评论 | ✅ 存在 | 第一轮调研取证页 |
| **精选弹窗** | `/jingxuan?modal_id={id}` | **全屏沉浸式 + 右侧评论栏** | ✅ **存在(已验证)** | 用户常用入口,见 §8 |
| 用户主页弹窗 | `/user/{secUid}?modal_id={id}&vid={id}` | 同精选弹窗 | ✅ **存在(已验证)** | 同精选 swiper 架构;`videoCount=3` 预加载更多 |

**关键结论:精选弹窗(`modal_id`)真正打开后,`window.player` 同样存在,且 `awemeId / uri / awemeInfo / seek()` 与详情页完全一致 —— 音频、字幕抓取逻辑无需为精选模式单独处理。** 详见 §8。

- ⚠️ 精选弹窗是 **swiper 信息流**:用户上下滑动切换视频时,URL 的 `modal_id` 与 `window.player` 会随之更新,且页面常预加载下一条(实测 `<video>` 元素有 2 个)。内容脚本需监听 SPA 路由 / `modal_id` 变化并以当前 `window.player.config.awemeInfo.awemeId` 为准重新取数(与 YouTube SPA 切换同类问题)。
- 平台识别建议:**按 `/video/` 路径 或 URL 含 `modal_id` 参数** 判定,而非只匹配 `/jingxuan`。

---

## 2. 音频(ASR)—— ✅ 完全可行

### 2.1 音频流位置
```js
window.player.config.awemeInfo.video.bitRateAudioList[0].urlList[0].src
```
是一个 **纯音频 DASH 流** 的 CDN 直链,形如:
```
https://v11-weba.douyinvod.com/<hash>/<exp>/video/tos/cn/tos-cn-ve-15/<id>/media-audio-und-mp4a/?a=6383&ch=0&cr=8&...
```
- 编码:`mp4a`(AAC),约 192k,`size = 13,159,583` 字节(≈13MB)
- 另有渐进式整片 MP4(`video.playAddr` / `video.playApi`,≈35MB)可做兜底,但**音频流更小,更适合 ASR**。

### 2.2 实测下载结果(从页面上下文 fetch 头部 64KB)

| 项 | 值 |
|---|---|
| HTTP 状态 | `206 Partial Content` |
| `content-range` | `bytes 0-65535/13159583` |
| `content-type` | `video/mp4` |
| 文件头(hex) | `00 00 00 20 66 74 79 70 69 73 6f 6d` → `....ftypisom`(合法 MP4 / ISO Base Media) |

CDN(`tencent-cos`)响应头关键项:
```
access-control-allow-origin: *
access-control-allow-headers: range
access-control-allow-methods: GET,HEAD,POST,OPTIONS
```
请求侧只需:
```
Referer: https://www.douyin.com/      # 页面 / background worker 自动带或可手动设
Range: bytes=...                       # 支持分段
```

**结论:音频直链开放 CORS + 支持 Range,内容脚本可直接 fetch,background worker 也可下载(设置 Referer 即可)。这条路与 B 站可用的 ASR 音频流性质一致。**

### 2.3 ⚠️ 必须避开的坑:同源 playApi 返回验证码页
`awemeInfo` 里还有一类**同源**播放地址:
```
https://www.douyin.com/aweme/v1/play/?file_id=...&sign=...&video_id=...&aid=6383
```
用 `fetch()` 请求它(无论是否带 Range)**返回的是 HTML 而非音频**:
```html
<html><head><title>验证码中间页</title>
<script src="https://lf-cdn-tos.bytescm.com/obj/static/sec_sdk_build/3.5.2/captcha/index.js"></script>
...
```
即字节码抖音风控的"验证码中间页"。媒体字节只流向播放器的 `<video>`/MSE 元素,不流向 `fetch()`。

> **务必使用 `bitRateAudioList[].urlList[].src` 的 douyinvod.com CDN 直链,不要用 `/aweme/v1/play/` 同源 playApi。**

### 2.4 与历史经验对照
项目记忆中 YouTube「后台抓音频被 Chrome 内核挂起堵死」是 YouTube 特例。抖音 CDN 行为(开放 CORS、支持 Range、`tencent-cos` 静态资源)更接近 B 站,**预期可复用现有 `FETCH_AUDIO` → background worker 下载 → base64 回传 → Whisper** 的链路。落地时仍建议先用 background worker 做一次完整下载验证。

---

## 3. 字幕 / CC —— ⚠️ 不可靠(多数视频拿不到文字)

### 3.1 平台层面:字幕功能存在
- 全局配置 `app.backendAbTest.subtitles`:
  ```json
  { "enable": 1, "language_list": [
      {"language_code":"zh-Hans-CN","language_id":1},
      {"language_code":"en-US","language_id":2} ],
    "only_oversea": 0, "scene_list": ["all"] }
  ```
- 播放器内置「字幕」控件:xgplayer 的 `XG-TEXT-TRACK` 插件(`<xg-text-track class="xg-text-track text-track-hide ...">`)。

### 3.2 本样本视频:没有 CC 轨
- `window.player.config.awemeInfo.claInfo === null`。
- 字幕菜单(`ul.xg-options-list`)里**只有一个选项**:
  ```html
  <li class="option-item selected" showtext="不开启" data-type="text-close" data-index="0">不开启</li>
  ```
  即没有任何语言轨可选。
- 画面底部出现的文字(如"…什么是编排吗")在 DOM 中**搜不到**,是**作者压制进视频画面的硬字幕(burned-in)**,机器不可读。

### 3.3 结论
- 只有当 Douyin/作者给视频**挂了 CC 轨**时,`claInfo` 才会带字幕文件 URL(预计为 WebVTT,可下载解析)。
- 这类视频比例不高,**远不如 B 站 CC / YouTube transcript 稳定**。
- 实践策略:**"检测到 `claInfo` 就解析字幕,否则回退 ASR(见 §2)"**。
- ⚠️ **待验证**:找一个**确实带 CC** 的抖音视频,确认 `claInfo` 内字幕文件的 URL 结构与格式(WebVTT / JSON)。

---

## 4. 评论舆情 —— ⚠️ 中等成本

- 互动计数可免签直接读:`awemeInfo.stats`
  ```json
  { "commentCount": 229, "diggCount": 2806, "shareCount": 636,
    "collectCount": 2655, "recommendCount": 39, ... }
  ```
- **评论正文不在 `window.player` 内存里**,需走 `GET /aweme/v1/web/comment/list/`(同样被 `a_bogus` 签名保护),或走 DOM 抓取。
- **精选弹窗模式利好(已验证)**:精选弹窗右侧会直接渲染评论区,DOM 可抓取,有稳定属性:
  ```
  [data-e2e="comment-list"]      # 评论列表容器
  [data-e2e="comment-item"]      # 单条评论
  [data-e2e="video-comment-more"]# 展开更多回复
  ```
  单条评论的用户名、正文、`时间·地点`、点赞数、回复数均在 DOM 中(正文在如 `.Sbe6bqNb` 之类的子节点,类名为混淆值需运行时定位)。**这比详情页更易抓取**。
- 方案优先级:
  1. **DOM 滚动抓取**(精选弹窗右栏 / 详情页评论区)—— 推荐,稳。
  2. 复刻 `a_bogus` 签名 —— 脆弱、易随版本失效,不推荐。
- 建议:评论功能放在最后实现,优先级低于摘要类主链路。

---

## 5. 抓包到的关键接口清单(参考)

| 用途 | 接口 / 来源 | 签名要求 | 对本扩展的取用方式 |
|---|---|---|---|
| 视频详情(标题/作者/play_addr/audio…) | `GET /aweme/v1/web/aweme/detail/` | `a_bogus` + `x-secsdk-web-signature` | **不直接调**,改读 `window.player.config.awemeInfo` |
| 纯音频流(ASR) | `…douyinvod.com/.../media-audio-und-mp4a/`(来自 `bitRateAudioList[].urlList[].src`) | 仅需 Referer | ✅ 直接 fetch / background 下载 |
| 渐进式整片 MP4(兜底) | `video.playAddr` / douyinvod.com CDN | 仅需 Referer | 备选 |
| 同源播放代理(❌坑) | `GET /aweme/v1/play/?...sign=...` | 风控 | ❌ 返回验证码页,勿用 |
| 评论列表 | `GET /aweme/v1/web/comment/list/` | `a_bogus` | DOM 抓取或破签名 |

---

## 6. 落地建议(优先级)

1. **P0 — 平台基础设施**
   - `PlatformFactory` 识别 `douyin.com/video` (及 `jingxuan?modal_id=`,待验证)。
   - `DouyinService` 实现 `detectVideo()`(读 `window.player.config.awemeInfo`:`awemeId / desc / authorInfo / video.duration / cover`)与 `seekTo()`(`window.player.seek()`)。
   - 内容脚本需在 **MAIN world** 才能读到 `window.player`(参照 YouTube 读 `ytInitialPlayerResponse` 的注入方式)。

2. **P0 — 音频 ASR 主链路**
   - `supportsDigitalASR() → true`;`getAudioUrl()` 返回 `bitRateAudioList[0].urlList[0].src`;`getAudioUrlCandidates()` 给出多 CDN 备份(`urlList` 通常含 v11/v26 多个域名)。
   - 复用 `FETCH_AUDIO` → background worker(带 Referer)→ Whisper。**先做一次完整下载验证**。

2.5. **P0/P1 — 优先复用官方 `chapterInfo`(强烈建议)**
   - 摘要/思维导图取数前先判 `awemeInfo.chapterInfo?.status === true`,若有则直接基于 `chapterAbstract + list[]` 生成(免 ASR、最快),`timestamp(ms)` 接 `seekTo()`。详见 §9。
   - 内容获取优先级:`chapterInfo` → `claInfo` 字幕 → 音频 ASR(见 §9.5)。

3. **P1 — 字幕增强**
   - `getSubtitles()`:若 `claInfo` 存在则解析其字幕文件;否则返回空 → 触发 ASR 回退。
   - 需先用带 CC 的视频确认 `claInfo` 格式。

4. **P2 — 评论**
   - DOM 滚动抓取评论区,映射为 `Comment[]`。

---

## 7. 待验证清单(下一步)

- [ ] 找一个**带 CC 轨**的抖音视频,确认 `claInfo` 字幕文件 URL 结构与格式。
- [x] 验证 `/jingxuan?modal_id=` 弹窗打开后 `window.player` 是否存在 —— ✅ 已验证一致,见 §8。
- [ ] 用 background worker 实测完整下载 13MB 音频流(确认不被风控/挂起)。
- [ ] 确认 `urlList` 内多 CDN 域名的可用性与 URL 时效(`x-expires` 等)。
- [ ] 实现精选 swiper 切换视频时的视频 ID 变更监听(SPA 路由 + `modal_id`)。

---

## 8. 精选弹窗(jingxuan)模式专项复核

> 背景:用户习惯通过"精选"浏览视频,其 URL(`/jingxuan?modal_id={id}`)与 UI(全屏沉浸式 + 右侧评论栏)和详情页完全不同。本节复核该模式下音频/字幕逻辑是否与详情页一致。
>
> 取证样本:同一视频 `7650813418760359203`,精选弹窗已打开态。
>
> 补充:博主主页弹窗(`/user/{secUid}?modal_id=&vid=`)亦已实测,与精选弹窗同为 swiper 架构,`window.player`/音频/字幕/评论全部一致(`videoCount=3` 预加载更多),不再单列。

**结论:音频与字幕抓取逻辑与详情页 100% 一致,无需为精选 / 博主主页弹窗单独适配。**

| 维度 | 详情页 `/video/{id}` | 精选弹窗 `/jingxuan?modal_id=` | 是否一致 |
|---|---|---|---|
| `window.player` | ✅ 存在(type `s`) | ✅ 存在(type `s`) | ✅ |
| `awemeId` | `7650813418760359203` | `7650813418760359203` | ✅ |
| `curDefinition.uri` | `v0d00fg10000d8micinog65lop…` 同 | 同 | ✅ |
| `seek()` / `duration` | ✅ / 543.893 | ✅ / 543.893 | ✅ |
| 音频流 `bitRateAudioList[0].urlList[0].src` | douyinvod CDN,`size=13159583` | douyinvod CDN,`size=13159583` | ✅ |
| `claInfo`(字幕) | `null` | `null` | ✅(均无 CC) |
| 字幕菜单选项 | 仅「不开启」 | 仅「不开启」 | ✅ |
| 屏幕字幕 | 硬字幕(DOM 取不到) | 硬字幕(DOM 取不到) | ✅ |

**差异点(精选模式更有利)**:

1. **评论可直接 DOM 抓取**:精选弹窗右侧实时渲染评论(`[data-e2e="comment-item"]`,见 §4),详情页则需滚动到下方评论区。精选模式对"评论舆情"功能更友好。
2. **swiper 信息流**:精选是上下滑动的连续流,切换视频时 `modal_id` 与 `window.player` 同步更新,且会预加载下一条(实测同时存在 2 个 `<video>`)。内容脚本须以**当前 `window.player.config.awemeInfo.awemeId` 为准**重新取数,并监听 `modal_id` 变化。
3. 抖音自带 AI 功能(右上「问AI」Tab、「识别画面」「听抖音」、"当前提及:…")可见,与本扩展功能有重叠,属竞品参考。

---

## 9. 官方 AI 章节总结 `chapterInfo`(重大利好)

> 背景:播放器左下角"章节要点"按钮,点开后右侧按时间轴展示"全片总述 + 各章节内容"。本节回答"无 CC 字幕时官方如何做到章节总结",并评估其对本扩展的价值。
>
> 取证样本:视频 `7623684607510088998`(@学海无涯「一个名字毁了一座城,盘点那些改名失败的城市」,7:50)。

### 9.1 抖音怎么做到的
**服务端 ASR(语音转文字)+ 大模型生成**,不依赖 CC 字幕。佐证:播放器提示"疑似使用了 AI 生成技术,请谨慎甄别";数据中 `showAiTag: true`、`useRecommendChapter: true`。结果以结构化对象 `chapterInfo` 随视频详情下发。

### 9.2 数据位置:免签可直接读
```js
window.player.config.awemeInfo.chapterInfo
```
实测结构(节选):
```js
{
  status: true,            // ★ 该视频是否有章节(覆盖率判断用)
  showAiTag: true,         // AI 生成标记
  useRecommendChapter: true,
  isPoint: true,
  chapterAbstract: "中国有10座城市因改名而失去了原有的古韵和文化底蕴,如南京、驻马店…",  // 全片总述
  list: [
    { desc:"引言",   timestamp:0,      points:[] },
    { desc:"南京",   timestamp:32500,  points:[{ desc:"・南京改名失金陵古韵",
        detail:"1. 古名底蕴：曾用名超40个,金陵为六朝古都…\n2. 现名不足：…", type:1 }] },
    { desc:"驻马店", timestamp:68000,  points:[{ detail:"1. 古名优势：古名汝南…\n2. 现名问题：…" }] },
    // … 每章: desc=标题, timestamp=毫秒(可直接喂 seekTo), points[].detail=要点正文
    { desc:"结语",   timestamp:437000, points:[] }
  ]
}
```
该字段随 `/aweme/v1/web/aweme/detail/` 一并下发,已在 `awemeInfo` 内 —— **无需额外接口、无需破签名、无需 ASR、无需下载音频**。

### 9.3 对本扩展的价值
1. **字段天然契合现有数据结构**:
   - `chapterAbstract` ≈ 现有 `SummaryResult.oneLiner / fullDigest`
   - `list[]`(`desc` + `timestamp(ms)` + `points[].detail`)≈ 现有 `chapters[]`,`timestamp` 直接可用于 `seekTo()`
2. **大幅缓解"字幕拿不到"**:对带章节的视频(用户反馈"绝大部分"都有),摘要 / 思维导图可直接基于 `chapterInfo` 生成,或将其作为素材交给本扩展 AI 做格式化 / 翻译 / 多语言输出。

### 9.4 边界与注意
- **是总结稿,非逐字转写**。AskAI 这类需要全文细节问答的场景仍可能需回退 ASR 拿完整文本;但 Summary / MindMap / 章节足够。
- **覆盖率非 100%**:必须判 `chapterInfo && chapterInfo.status === true`;无则回退"音频 ASR(§2)"。
- 内容为**中文**;多语言 UI 下需经本扩展 AI 翻译。
- 是抖音 AI 生成,质量不保证 100% 准确(官方亦提示"谨慎甄别")。

### 9.5 建议的内容获取优先级(取数链)
```
1) chapterInfo.status===true        → 直接用官方章节总结(免费、最快)          [§9]
2) 否则 claInfo 存在                 → 解析 CC 字幕文本 → 走本扩展 summarizer  [§3, 待验证]
3) 否则                             → 下载音频流 → Whisper ASR → summarizer    [§2]
```

---

*取证副产物:`.codex/douyin_detail.network-response`(详情接口完整响应,供查阅,可删)。*
